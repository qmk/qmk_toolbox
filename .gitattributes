# Normalize line endings
* text=auto

# Binary resources — prevent CRLF conversion that would corrupt executables and shared libraries
resources/**/* -text

# Re-enable text handling for known text files under resources/
resources/**/*.conf text
resources/**/*.eep text
resources/**/*.iss text
resources/**/*.plist text
resources/**/*.rules text
resources/**/*.txt text
resources/**/*_release_* text
